version: '3.7'

services:

  whoami:
    image: containous/whoami
    container_name: whoami
    restart: unless-stopped
    networks:
      - caddy
    labels:
      caddy: "${DOMAIN}"
      caddy.reverse_proxy: "{{upstreams 80}}"

networks:
  caddy:
    external: true